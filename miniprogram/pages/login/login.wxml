<view class="loadingPage">
  <view class="cover-page">
    <view class='begin'>
      <block wx:if="{{hasStorage}}">
        <button>即将进入VR试衣</button>
      </block>
      <block wx:else>
        <button type="primary" open-type="getUserInfo" bindgetuserinfo="getUser">微信用户授权登录</button>
      </block>
    </view>
    <view class="tips">
      授权代表你已经同意<text bindtap="openDoc">“小程序用户协议”</text>
    </view>
  </view>
</view>
